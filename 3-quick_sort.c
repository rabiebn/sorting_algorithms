#include "sort.h"

/**
 * quick_sort - Quick sort an array of ints.
 * 
 * @array: array of ints to sort;
 * @size: number of elements in @array.
*/
void quick_sort(int *array, size_t size)
{
    size_t i, pivot = size / 2;

    for (i = 0; i < size; i++)
    {
        
    }
}